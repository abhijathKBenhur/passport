const CompanySchema = require("../db-config/Company.schema");

export const addTransaction = (request) =>  { 
    
}

export const getTransactions = (request) =>  { 
    
}


const TransactionAPI = {
    addTransaction,
    getTransactions
}

export default TransactionAPI